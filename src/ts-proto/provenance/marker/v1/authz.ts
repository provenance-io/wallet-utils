// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v5.29.3
// source: provenance/marker/v1/authz.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import { Coin } from "../../../cosmos/base/v1beta1/coin";

export const protobufPackage = "provenance.marker.v1";

/**
 * MarkerTransferAuthorization gives the grantee permissions to execute
 * a marker transfer on behalf of the granter's account.
 */
export interface MarkerTransferAuthorization {
  /** transfer_limit is the total amount the grantee can transfer */
  transferLimit: Coin[];
  /**
   * allow_list specifies an optional list of addresses to whom the grantee can send restricted coins on behalf of the
   * granter. If omitted, any recipient is allowed.
   */
  allowList: string[];
}

function createBaseMarkerTransferAuthorization(): MarkerTransferAuthorization {
  return { transferLimit: [], allowList: [] };
}

export const MarkerTransferAuthorization: MessageFns<MarkerTransferAuthorization> = {
  encode(message: MarkerTransferAuthorization, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.transferLimit) {
      Coin.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.allowList) {
      writer.uint32(18).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MarkerTransferAuthorization {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMarkerTransferAuthorization();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.transferLimit.push(Coin.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.allowList.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MarkerTransferAuthorization {
    return {
      transferLimit: globalThis.Array.isArray(object?.transferLimit)
        ? object.transferLimit.map((e: any) => Coin.fromJSON(e))
        : [],
      allowList: globalThis.Array.isArray(object?.allowList)
        ? object.allowList.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: MarkerTransferAuthorization): unknown {
    const obj: any = {};
    if (message.transferLimit?.length) {
      obj.transferLimit = message.transferLimit.map((e) => Coin.toJSON(e));
    }
    if (message.allowList?.length) {
      obj.allowList = message.allowList;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MarkerTransferAuthorization>, I>>(base?: I): MarkerTransferAuthorization {
    return MarkerTransferAuthorization.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MarkerTransferAuthorization>, I>>(object: I): MarkerTransferAuthorization {
    const message = createBaseMarkerTransferAuthorization();
    message.transferLimit = object.transferLimit?.map((e) => Coin.fromPartial(e)) || [];
    message.allowList = object.allowList?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
